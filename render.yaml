services:
  - type: web
    name: pdr-airchina-v1-0
    runtime: python
    repo: https://github.com/siyaofu3931/PDR_AIRCHINA.git
    branch: main
    buildCommand: pip install --upgrade pip && pip install fastapi "uvicorn[standard]"
    startCommand: uvicorn backend.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    pullRequestPreviewsEnabled: false
